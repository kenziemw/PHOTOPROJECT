<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Art | Captured Moments</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            height: 100%; /* Ensure full height for body and html */
        }

        body {
            display: flex;
            flex-direction: column; /* Flexbox layout for sticky footer */
            background-color: #fff;
            color: #333;
            font-family: 'Georgia', serif;
            font-size: 16px;
            line-height: 1.6;
        }

        main {
            flex: 1; /* Push footer to the bottom */
        }

        /* Header */
        header {
            position: relative; /* Needed for nav-right positioning */
        }
        .header {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 20px;
        }

        .site-title {
            font-size: 2.5rem;
            letter-spacing: 0.1rem;
            font-weight: 400;
            text-transform: uppercase;
            text-align: center;
            color: #333;
        }

        .nav {
            margin-top: 1rem;
            width: 100%;
            max-width: 1200px;
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 0 2rem;
        }

        .nav-left {
            display: flex;
            gap: 2rem;
        }

        .nav-right {
            position: absolute;
            right: 2rem;
        }

        .nav-link {
            text-decoration: none;
            color: #333;
            font-size: 1rem;
            font-weight: 300;
            letter-spacing: 0.05rem;
            transition: color 0.3s ease;
        }

        .nav-link:hover {
            color: #888;
        }

        .login-button {
            font-weight: bold;
        }

        /* Image Section */
        .gallery-section {
            margin: 40px auto;
            max-width: 1200px;
            padding: 20px;
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
        }

        .gallery-item {
            flex: 1 1 calc(33.333% - 20px);
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
        }

        .gallery-item img {
            width: 100%;
            max-width: 300px;
            border-radius: 10px;
            transition: transform 0.3s ease;
        }

        .gallery-item img:hover {
            transform: scale(1.05);
        }

        .gallery-item h3 {
            margin-top: 10px;
            font-size: 1.2rem;
            color: #555;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 20px;
            background-color: #333;
            color: #fff;
        }

       /* New nav-button styling */
       .nav-button {
            background-color: #ddd;
            color: #333;
            padding: 6px 12px;
            border-radius: 5px;
            text-decoration: none;
            border: none;
            font-size: 1rem;
            font-weight: 300;
            display: inline-block; /* ensures it's treated like a button */
            transition: background-color 0.3s ease;
        }

        .nav-button:hover {
            background-color: #bbb;
        }

    </style>
</head>
<body>
    <header>
        <div class="header">
            <h1 class="site-title">ART</h1>
            <nav class="nav">
                <div class="nav-left">
                    <a href="/landing" class="nav-link">home</a>
                    <a href="/art" class="nav-link">art</a>
                    <a href="/about" class="nav-link">about</a>
                    <a href="<%= isLoggedIn ? '/inquire' : '/login?redirect=/inquire' %>" class="nav-link">inquire</a>
                    <% if (isPhotographer) { %>
                        <a href="/requests" class="nav-link">requests</a>
                    <% } %>
                </div>
                <div class="nav-right">
                    <% if (isLoggedIn) { %>
                        <!-- Apply both nav-link and nav-button classes -->
                        <a href="/logout" class="nav-link nav-button login-button">logout</a>
                    <% } else { %>
                        <a href="/login" class="nav-link nav-button login-button">login</a>
                    <% } %>
                </div>
            </nav>
        </div>
    </header>
    <main>
        <section class="gallery-section">
            <div class="gallery-item">
                <img src="/images/euro.jpg" alt="Art 1">
            </div>
            <div class="gallery-item">
                <img src="/images/family.jpg" alt="Art 2">
            </div>
            <div class="gallery-item">
                <img src="/images/earth.jpg" alt="Art 3">
            </div>
            <div class="gallery-item">
                <img src="/images/night.jpg" alt="Art 4">
            </div>
            <div class="gallery-item">
                <img src="/images/ocean.jpg" alt="Art 5">
            </div>
            <div class="gallery-item">
                <img src="/images/chinese.jpg" alt="Art 6">
            </div>
        </section>
    </main>
    <footer>
        <p>&copy; 2024 Captured Moments. All rights reserved.</p>
    </footer>
</body>
</html>
